# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository (`cp-chula-coursework/2110597-GRAPH-SYS-ARCH`) is currently empty — it contains only a `README.md` placeholder, an MIT `LICENSE`, and a standard Python `.gitignore` template. No source code, build tooling, or tests have been added yet.

The `.gitignore` is scoped for a Python project (bytecode, virtualenvs, pytest/mypy/ruff caches, Jupyter checkpoints, etc.), suggesting coursework content will likely be Python-based, but no dependency manifest (`requirements.txt`, `pyproject.toml`) or source layout exists yet to confirm this.

There are no build, lint, or test commands to document until code is added to the repository. When course material is added, update this file with the actual commands and architecture notes.
